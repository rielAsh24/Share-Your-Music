type activity = {
  name: string;
  date: string;
};

type form_data = {
  Name: string;
  Email: string;
  Username: string;
  Password: string;
  Comments: string;
  Mode: string;
};

export { activity, form_data };
