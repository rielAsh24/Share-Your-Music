{
  "extends": ["//"],
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["build", "^dev"],
      "persistent": true,
      "cache": false
    }
  }
}
